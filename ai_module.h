// ai_module.h
// This is called an "Include Guard". It prevents the compiler from reading this menu twice.
#ifndef AI_MODULE_H
#define AI_MODULE_H

// We tell main.c that this function exists somewhere else
int lsh_suggest(char **args);

#endif